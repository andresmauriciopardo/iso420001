---
scope: Plataforma SaaS de gobernanza de IA ISO/IEC 42001 (MVP 24 cambios OpenSpec + extensión fase 2)
purpose: build-substrate
altitude: initiative
mode: headless, Fast path, sin preguntas; inferencias marcadas [ASSUMPTION]
updated: 2026-09-17T23:53
---

- (event) Activación headless (Fast path). resolve_customization y resolve_config OK; sin overrides en _bmad/custom para bmad-architecture. Intent=create, altitude=initiative, purpose=build-substrate. Insumos: CLAUDE.md, PROMPT_REVIEW_AND_ADJUSTMENTS A4/A6-A9, PRD §6-§9/§16-§18 + addendum §B-§D, DOMAIN_MAP, ROADMAP §3, TRACEABILITY_MODEL, P2 §4/§16-§17/§40-§45/§62/§65, resumenes/01 §4 y 02 §4.12 (verificación).
- (constraint) CLAUDE.md fija stack por defecto (ADR-001), tenant_id+RLS (ADR-002), monolito modular+eventos+outbox (ADR-003), campos comunes de entidad, audit log append-only con hash chain, migraciones únicamente, pruebas por cambio, secretos vía abstracción, código/API en inglés, docs en español, UI bilingüe.
- (constraint) A4: resumenes/ nunca se importa en runtime; semilla desde packages/standards-seed (dataset estructurado revisado por humanos). A6: semilla sin texto literal; campos identifier,title,requirement_summary,record_type,expected_evidence,references,standard_version. A7: StandardVersion soporta 42001:2023+Amd1:2024, UNE 2025 identical_adoption, 19011:2026, 23894:2023 parcial, 42006 pendiente.
- (constraint) A8: PostgreSQL compartido + RLS obligatoria + prefijo de almacenamiento por tenant; se descarta esquema-por-tenant; pg-boss con interfaz de jobs abstracta; cadena de hashes por tenant. A9: plataforma registrada como sistema de IA (A.6, A.8, A.9), WCAG 2.2 AA, residencia/RPO/RTO/legal hold, coste LLM por tenant, pruebas de dominio con fixtures de resumenes/, pruebas de contrato de conectores.
- (constraint) PRD §8.4/§17 D-3: catálogo global (Requirement, Control) separado del estado por tenant (RequirementImplementation, SoAItem, ControlImplementation). D-4: Finding genérico con source_type; AIProvider≠Supplier≠Integration. D-6: matriz rol×permiso del addendum §B como punto de partida (requiere aprobación). §8.6: nombres de evento MAYUSCULAS_CON_GUION_BAJO; Administration único escritor del registro de auditoría.
- (constraint) Invariantes PRD §9 (PR-DR-001..030) deben ser imposibles de violar en código y tener prueba unitaria; PR-DR-009 prohíbe la palabra certificado; PR-DR-010 exige fórmula/numerador/denominador/exclusiones/fuentes en toda puntuación; PR-DR-011 separación de funciones en evidencia.
- (constraint) Hechos normativos verificados en resumenes/02 §4.12: 38 controles (A.2:3, A.3:2, A.4:5, A.5:4, A.6:9, A.7:5, A.8:4, A.9:3, A.10:3); resumenes/01 §4 cláusulas 4.1-10.2 según encabezados. Ningún identificador adicional se inventa.
- (decision) AD-1 Paradigma: monolito modular con arquitectura hexagonal ligera por bounded context (puertos/adaptadores): dominio en packages/domain-<ctx>, entrega en apps/web (Next.js), persistencia vía packages/db (Prisma). Binds: all. Prevents: lógica de dominio dispersa en route handlers/UI. [ADOPTED por CLAUDE.md/A8]
- (decision) AD-2 Aislamiento de tenant: tenant_id NOT NULL en toda tabla de negocio; RLS FORCE con política por current_setting('app.tenant_id'); rol de aplicación sin BYPASSRLS; contexto de tenant fijado con SET LOCAL en cada transacción; catálogo global sin tenant_id y de solo lectura para el rol de aplicación; storage con prefijo tenants/<tenant_id>/. [ADOPTED]
- (decision) AD-3 Reglas de dependencia: capas kernel → platform → domain → app; entre contextos solo puertos públicos (index.ts) y eventos; ningún contexto toca tablas de otro (repositorios propios); Reporting/Administration/AI Assistance leen mediante puertos de lectura (read models), no por acceso directo a tablas. Verificado por prueba de arquitectura (dependency-cruiser) en CI. [ASSUMPTION herramienta]
- (decision) AD-4 Mutación de estado solo vía servicios de aplicación (commands) con secuencia autorizar→validar(Zod)→transacción{mutar; escribir outbox}; cada mutación emite al menos un evento de dominio o ENTITY_MUTATED; Administration consume el outbox completo y escribe AuditEntry (único escritor). [ADOPTED ADR-003/005]
- (decision) AD-5 Outbox + pg-boss: tabla outbox_events en la misma transacción; relay a pg-boss; consumidores idempotentes con tabla processed_events(consumer, event_id); reintentos exponenciales; dead-letter; correlation_id y causation_id propagados. Puerto JobScheduler abstrae pg-boss. [ADOPTED ADR-004]
- (decision) AD-6 Registro de auditoría: audit_entries append-only (triggers que rechazan UPDATE/DELETE, sin privilegios de escritura salvo INSERT para el rol), hash_n = SHA-256(hash_{n-1} || canonical_json(entry)) por tenant, secuencia por tenant, procesamiento serializado por tenant, verificación por API/UI y exportación. Ancla externa diaria diferida a F2. [ADOPTED ADR-005]
- (decision) AD-7 Catálogo normativo global versionado separado del estado por tenant; el estado referencia el catálogo por (standard_version_id, identifier estable). Publicar una nueva StandardVersion nunca modifica filas de tenant. [ADOPTED PRD D-3, pendiente ratificación humana]
- (decision) AD-8 Autorización: catálogo de permisos <resource>.<action> como única fuente (packages/kernel), RBAC con roles del tenant, alcance tenant/unidad/objeto (propiedad); toda operación declara permiso requerido y se verifica en el servicio de aplicación (nunca solo en UI); acceso a objeto siempre por repositorio con tenant fijado (BOLA). Matriz por defecto = addendum §B (D-6, requiere aprobación).
- (decision) AD-9 La SoA gobierna los flujos: solo Controls & SoA emite CONTROL_ACTIVATED/DEACTIVATED/CONTROL_APPLICABILITY_CHANGED; los demás contextos reaccionan a los eventos o consultan SoAReadPort.isControlActive; ningún contexto lee SoAItem directamente ni infiere aplicabilidad. Not Applicable no genera tareas, evidencia recurrente ni métricas (PR-DR-001..004).
- (decision) AD-10 Aprobaciones: toda decisión de cumplimiento pasa por Approval (Workflow) con actor, motivo, valor anterior/nuevo; el contexto dueño ejecuta la transición al consumir APPROVAL_GRANTED o de forma sincrónica vía ApprovalPort; separación de funciones verificada en el servicio (PR-DR-011) con excepción registrada para tenants pequeños (addendum D.4, pendiente aprobación).
- (decision) AD-11 Versionado: entidades con version (concurrencia optimista, If-Match); documentos aprobados (AIMSScope, Policy, StatementOfApplicability, ImpactAssessment, RiskMethodology, Document) con tabla *_versions inmutable por aprobación; historial genérico ObjectVersion (snapshot JSON) para FR-009. Soft delete con deleted_at; borrado físico solo por job de retención sin legal hold (F2).
- (decision) AD-12 Enumeraciones canónicas en inglés en packages/kernel/enums (única fuente, reflejadas en Prisma enums); i18n con claves enum.<Entity>.<Value>; mensajes en packages/i18n/messages/{es,en}; prueba de cero claves faltantes; términos prohibidos (certified, compliance score) verificados por prueba.
- (decision) AD-13 Almacenamiento de objetos vía puerto ObjectStorage (S3-compatible; MinIO en local); clave tenants/<tenant_id>/<domain>/<object_id>/<version>; SHA-256 calculado en servidor; URLs firmadas de corta duración; escaneo antivirus asíncrono antes de Collected. [ASSUMPTION MinIO/ClamAV]
- (decision) AD-14 Secretos vía puerto SecretStore; la BD guarda solo secret_ref; adaptadores: env/local cifrado (dev), KMS/Vault (prod, F2); eventos CREDENTIAL_*; nunca en cliente.
- (decision) AD-15 API: Route Handlers de Next.js bajo /api/v1/<domain> (P2 §43), Zod en entrada/salida, OpenAPI generado desde Zod, errores application/problem+json (RFC 9457) con code canónico, paginación por cursor, Idempotency-Key en POST, permiso declarado por endpoint, auditoría automática de mutaciones. [ASSUMPTION prefijo v1]
- (decision) AD-16 Puntuaciones e informes: ScoreSnapshot con fórmula, numerador, denominador, exclusiones, registros fuente, timestamp; ReportDefinition declarativa sobre un QueryModel con datasets nombrados expuestos por puertos de lectura (sin SQL libre); ReportRun asíncrono con hash; render HTML→PDF en worker con Chromium headless. [ASSUMPTION render]
- (decision) AD-17 IA asistida: puerto LLMProvider en packages/ai-core; el dominio no importa SDKs de proveedor; toda invocación explícita crea AIAssistanceTask y LLMUsageRecord; AIGeneratedArtifact con metadatos y estado de revisión; el principal de IA solo tiene permisos *.suggest (sin escritura sobre estados de cumplimiento); la plataforma se registra como AISystem del tenant plataforma. Modelos Claude por defecto sin acoplar.
- (decision) AD-18 SDK de conectores: pipeline Connector Interface → Provider Adapter → Credential (SecretStore) → Connection Test → Sync Job → Normalization → Mapping → Evidence/Candidate; estados de conector mock/configured/validated; disponibilidad de dato por campo; taxonomía de errores P2 §62; adaptadores en packages/connectors-*, nunca en el dominio.
- (decision) AD-19 Observabilidad: OpenTelemetry (trazas, métricas, logs estructurados); correlation_id generado en el borde y propagado a outbox, jobs y AuditEntry; health endpoints web/worker; job monitoring de pg-boss expuesto.
- (decision) AD-20 Despliegue: una imagen Docker con MODE=web|worker; docker-compose para local (postgres, minio, clamav, otel-collector, mailpit); entornos local/ci/staging/production; migraciones Prisma como paso previo al despliegue; proveedor de nube neutral (S3-compatible, PostgreSQL gestionado) pendiente de decisión humana (Q6).
- (decision) AD-21 Retención y legal hold: Administration posee RetentionPolicy, LegalHold y el job de purga; ningún registro de cumplimiento se borra en silencio; purga física solo en F2 tras retención vencida sin legal hold y con aprobación.
- (decision) AD-22 Identificadores UUID v7, timestamps timestamptz UTC, JSON canónico (RFC 8785) para hashes, nombres de tabla snake_case plural, columnas snake_case, entidades PascalCase, eventos UPPER_SNAKE, permisos resource.action.
- (decision) AD-23 Pruebas: Vitest unitarias por paquete (invariantes PR-DR con prueba nombrada por ID), integración con PostgreSQL real en contenedor (RLS, API), Playwright E2E (15 pasos §16.1), suite de seguridad (cross-tenant, escalada, BOLA, secretos), pruebas de dominio de la semilla (38/26/11/7), pruebas de arquitectura (dependencias, 'nuevo informe sin código', 'nuevo conector sin tocar dominio').
- (assumption) Herramientas no fijadas por insumos y adoptadas provisionalmente: pnpm+Turborepo, next-intl, Auth.js o better-auth (pendiente verificación web), MinIO, ClamAV, Chromium headless (Playwright) para PDF, dependency-cruiser, Testcontainers. Todas marcadas [ASSUMPTION] y llevadas a ADR.
- (question) Q-A1 Prefijo /api/v1 vs /api sin versión (P2 §43 no versiona). Q-A2 Proveedor de nube/región (Q6 PRD). Q-A3 Aprobación de la matriz rol×permiso (D-6). Q-A4 Ratificar D-3 separación catálogo/estado. Q-A5 Excepción de separación de funciones en tenants pequeños (D.4). Q-A6 Exception vive en Controls & SoA (propuesta). Q-A7 Competency en AIMS hasta F2.
- (event) Memlog de decisiones base completo; inicio de destilado de la espina y documentos de arquitectura.
- (version) Verificado en web 2026-09-17 (registry.npmjs.org, endoflife.date, sitios oficiales): Node 24.21 LTS (26 LTS en oct-2026); TypeScript 7.0.2; Next.js 16.3.5; React 19.3.0; Tailwind 4.3.3; shadcn CLI 4.21 (Base UI por defecto); PostgreSQL 18.6 (19 en beta); Prisma 7.10 (8 RC); Zod 4.6.5; pg-boss 12.33.1; OTel api 1.9.1 / sdk-trace-node 2.11 / sdk-node 0.222; Vitest 5.0.1; Playwright 1.63; pnpm 12.4 (Rust); Turborepo 2.10; next-intl 4.14.5; zod-openapi 6.0.2; ClamAV 1.5.4 (LTS 1.4.6); Docker 29.8; Mermaid 12; @anthropic-ai/sdk 0.126; ExcelJS 4.4 (MIT, sin release desde 2023); Sentry nextjs 10.75.
- (decision) Autenticación: better-auth 1.7 (plugins OIDC genérico + twoFactor TOTP) en lugar de Auth.js/next-auth: Auth.js está en modo mantenimiento bajo el equipo de Better Auth desde sep-2025 y v5 no tendrá GA ni TOTP nativo. Riesgo: peer deps declaran Prisma 5-7 (no 8). [ASSUMPTION → ADR-011, pendiente aprobación humana por ser arquitectura de seguridad]
- (decision) Almacenamiento de objetos local/autoalojado: SeaweedFS 4.47 (Apache-2.0) en lugar de MinIO (repo archivado 2026-04-25, CE en mantenimiento; Garage es AGPLv3 y CLAUDE.md exige aprobación para copyleft). En producción, servicio S3-compatible gestionado. [ASSUMPTION → ADR-008]
- (decision) Modelos Claude verificados en docs oficiales 2026-09-17: claude-fable-5-1, claude-opus-5, claude-sonnet-5, claude-haiku-4-5 (snapshot claude-haiku-4-5-20251001). Se usan solo como valor por defecto configurable de LLMProviderConfig; el dominio no los referencia.
- (decision) XLSX (F2) con ExcelJS (MIT); el paquete npm xlsx (SheetJS) se descarta por CVEs sin corrección en npm. PDF: HTML→PDF con Playwright/Chromium en worker (ADR-009). OpenAPI: zod-openapi 6 (peer zod 4).
- (event) Documentos producidos: docs/architecture/{SYSTEM,DOMAIN,DATA,API,SECURITY,EVENT,INTEGRATION,REPORTING,AI}_ARCHITECTURE.md; docs/decisions/ADR-001..ADR-016; docs/compliance/TRACEABILITY_MATRIX.md (esqueleto cambios 1-5). API/SECURITY, EVENT/INTEGRATION, REPORTING/AI y ADR-007..016 redactados por subagentes a partir de la espina. lint_spine.py: 0 hallazgos.
- (assumption) Subagentes (API/SECURITY): formato de API key aims_<env>_<prefix>_<secret> con SHA-256 y rotación 24h; catálogo de errores ampliado; If-Match también en POST de transición; rate limits 100/min sesión, 600/min API key, 30/min IP auth con contadores en tabla UNLOGGED; SupportAccessGrant ≤72h; argon2id, sesión 12h/60min inactividad; cifrado de sobre con DEK por tenant AES-256-GCM; magic bytes MIME, cuarentena; Renovate, CodeQL, gitleaks, CycloneDX, cosign; pentest externo antes del piloto. Todo pendiente de aprobación humana (seguridad).
- (assumption) Subagentes (EVENT/INTEGRATION): relay polling 500ms lotes 100 FOR UPDATE SKIP LOCKED por tenant; columnas seq y trace_context en outbox; cursor audit_trail_cursors; retención outbox 30d/processed_events 90d; permisos administration.jobs.view/manage; debounce 60s kpi-recalculation; registros normalizados Normalized*; IntegrationMapping versionado; MODEL_VERSION_DRIFT como IntegrationFinding (emitir MODEL_VERSION_CHANGED exige command humano); validated exige Approval; prefijo de secret_ref por tenant; egress permitido por lista.
- (assumption) Subagentes (REPORTING/AI): fórmulas concretas de las 6 puntuaciones y ExclusionReason; permisos por dataset y *.suggest por recurso; umbral alto riesgo configurable; report builder F3; adaptadores llm-anthropic/llm-openai/llm-azure-openai; modelos recomendados por tarea (opus-5, sonnet-5, haiku-4-5); TTL caché 24h; perfiles de redacción none|pii_basic|strict; MVP de IA = puerto + FakeLLMProvider + registro + AISystem 'Platform AI Assistant' sin UI.
- (event) Inicio del Reviewer Gate (headless, nunca se omite): rubric walker + 2 finalize_reviewers + lentes ad hoc de seguridad/cumplimiento y de reconciliación de insumos, en paralelo, salidas en reviews/.
